# Deixe vazio em desenvolvimento (o proxy do Vite encaminha /api ao backend).
# Em produção, aponte para a URL pública do backend, por exemplo:
# VITE_API_BASE=https://api.seudominio.com.br
VITE_API_BASE=
