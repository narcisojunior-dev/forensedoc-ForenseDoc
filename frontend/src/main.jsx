import React from "react";
import { createRoot } from "react-dom/client";
import ForenseDoc from "./ForenseDoc.jsx";

createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <ForenseDoc />
  </React.StrictMode>
);
